/healthz:
  get:
    summary: Health Check
    description: Reliable endpoint to check API health
    operationId: healthz
    tags:
      - endpoint
    responses:
      '200':
        description: 'OK'
        content:
          application/json:
            schema:
              type: object
              properties:
                healthy:
                  type: boolean
                  example: true
                typesense_healthy:
                  type: boolean
                  example: true
                postgres_healthy:
                  type: boolean
                  example: true
